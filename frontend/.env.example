# Salin file ini menjadi `.env` lalu sesuaikan bila perlu.
# Base URL API yang dipakai semua pemanggilan di src/api.js.
# Default "/api" cocok untuk reverse proxy (nginx) maupun vite dev proxy.
VITE_API_BASE=/api
